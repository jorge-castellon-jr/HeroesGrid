{
  "name": "heroes-grid",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "sync-versions": "node scripts/sync-versions.js",
    "dev": "yarn sync-versions && turbo dev",
    "build": "yarn sync-versions && turbo build",
    "lint": "turbo lint",
    "preview": "turbo preview"
  },
  "packageManager": "yarn@4.8.1",
  "dependencies": {
    "turbo": "^2.6.0"
  },
  "devDependencies": {
    "shadcn": "^3.5.0"
  }
}
